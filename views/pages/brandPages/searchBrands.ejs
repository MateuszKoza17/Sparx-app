<nav class="navbar navbar-expand-lg navbar-dark mt-5">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/">Sparx.</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/jobs">Jobs</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/faq">Faq</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/search">Search</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-bold" href="/app/getStarted">Get Started</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="searchBrands__container">
  <div>
    <form method="GET" class="my-5 d-flex">
      <input
        type="text"
        class="form-control"
        name="q"
        value="<%- query.q %>"
        placeholder="Search brands"
      />
      <button class="btn btn-dark ms-3">Search</button>
    </form>

    <div class="mt-4">
      <% if (query.q) { %>
      <p>Search results for query: <%= query.q %></p>
      <% } %>
    </div>
  </div>

  <div class="mt-5">
    <p>
      Default Option:
      <a href="/brands/csv" class="text-primary fw-bold">Generate Brands CSV</a>
    </p>
  </div>

  <% brands.forEach(brand => { %>
  <div class="card m-5">
    <div class="card-body">
      <div class="d-flex align-items-center">
        <div class="user__img--profile me-3">
          <h4 class="text-center fw-bold"><%= brand.name.slice(0,2) %></h4>
        </div>
        <div class="d-flex flex-column">
          <h5 class="card-title fw-bold">Company: <%= brand.name %></h5>
          <p>Slug: <%= brand.slug %></p>
        </div>
      </div>
      <a href="/brand/<%= brand._id %>" class="btn btn-dark mt-4"
        >Show Profile</a
      >
      <a href="/account/brand/dashboard/settings/edit" class="btn btn-outline-warning mt-4"
        >Edit Brand</a
      >
    </div>
  </div>
  <% }); %>
</div>
